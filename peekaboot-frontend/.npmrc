# Both build systems run `npm ci` through a plugin that shows npm's output only when it
# fails, so the audit and funding summaries are noise nobody reads. Shaping it here rather
# than in the two build files keeps the setting single-sourced.
audit=false
fund=false
