{
  "properties": [
    {
      "name": "peekaboot.lifecycle.enabled",
      "type": "java.lang.Boolean",
      "defaultValue": true,
      "description": "Enables the application-ready startup summary, the matching shutdown summary and the run history behind the Lifecycle tab."
    }
  ]
}
