spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  docker:
    compose:
      enabled: false

peekaboot:
  enabled: true
  dev-toolbar: true

management:
  tracing:
    sampling:
      probability: 1.0
